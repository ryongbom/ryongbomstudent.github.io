// 2.9. example.cpp : Defines the entry point for the console application.
//

#include "stdafx.h"
#include <iostream>
using namespace std;


/*int main()
{
	int digit[50];
	int dem = 1, num = 97;
	int i = 0;
	int pre;

	pre = dem / num;
	dem = (dem % num) * 10;

	while (i < 50)
	{
		digit[i] = dem / num;
		dem = (dem % num) * 10;
		++i;
	}

	cout << pre;
	for(i = 0; i < 50; i++)
		cout << digit[i];
	cout << endl;

	printf("%.50f", (double)1 / 97);

	return 0;
}*/

int main()
{
	char n = 0;
	while (n++ >= 0);
	cout << n << endl;
	return 0;
}

