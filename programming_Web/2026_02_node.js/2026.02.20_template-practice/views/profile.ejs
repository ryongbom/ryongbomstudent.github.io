<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <title><%= title %> - 프로필</title>
    <style>
        body { font-family: Arial; margin: 40px; }
        .profile-card {
            border: 1px solid #ddd;
            padding: 20px;
            max-width: 400px;
            border-radius: 10px;
            box-shadow: 0 2px 5px rgba(0,0,0,0.1);
        }
        h1 { color: #333; }
        .job { color: #666; font-weight: bold; }
        .hobby { 
            background: #f0f0f0; 
            padding: 5px 10px; 
            border-radius: 5px;
            display: inline-block;
        }
    </style>
</head>
<body>
    <div class="profile-card">
        <!-- 변하는 부분: 데이터로 채움 -->
        <h1><%= user.name %></h1>
        <p class="job">직업: <%= user.job %></p>
        <p>나이: <%= user.age %>살</p>
        
        <!-- 취미 목록 (배열 처리) -->
        <p>취미: 
            <% user.hobbies.forEach(hobby => { %>
                <span class="hobby"><%= hobby %></span>
            <% }) %>
        </p>
        
        <!-- 조건문 예시 -->
        <% if (user.isOnline) { %>
            <p style="color: green;">🟢 온라인</p>
        <% } else { %>
            <p style="color: gray;">⚪ 오프라인</p>
        <% } %>
        
        <!-- 날짜 표시 -->
        <p style="font-size: 12px; color: #999;">
            마지막 업데이트: <%= new Date().toLocaleString() %>
        </p>
    </div>
</body>
</html>